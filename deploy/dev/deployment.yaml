apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet-ingester
  namespace: experiment
  labels:
    app: dotnet-ingester
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 100%
      maxSurge: 100%
  replicas: 2
  selector:
    matchLabels:
      app: dotnet-ingester
  template:
    metadata:
      labels:
        app: dotnet-ingester
        app.kubernetes.io/name: dotnet-ingester
        app.kubernetes.io/component: server
        log-source: dcsp-backend
        team: platform
    spec:
      terminationGracePeriodSeconds: 30
      nodeSelector:
        pool_type: general
      tolerations:
        - key: "kubernetes.azure.com/scalesetpriority"
          operator: "Equal"
          value: "spot"
          effect: "NoSchedule"
      containers:
        - name: ingester
          image: acrvcedcsp.azurecr.io/experiment/ingestion/dotnet-ingester:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: global-env-config
            - configMapRef:
                name: dotnet-ingester-config
            - secretRef:
                name: dotnet-ingester-kv-experiment-config
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 768m
              memory: 768Mi
            requests:
              cpu: 50m
              memory: 384Mi
